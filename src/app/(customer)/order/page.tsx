export default function OrderEntryPlaceholder() {
  return (
    <main className="flex min-h-screen items-center justify-center p-6">
      <p className="text-sm text-neutral-500">
        Table QR landing page — built in Sprint 1
        (validates ?table= &amp; token=, then shows the menu).
      </p>
    </main>
  );
}
